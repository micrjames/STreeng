import { STreeng } from "../STreeng";

describe("A tree", () => {
   let streeng: STreeng;
   beforeAll(() => {
	  streeng = new STreeng();
   });
   describe("With no nodes", () => {
	  test.todo("Should exist.");
	  test.todo("Should entail an empty string.");
	  test.todo("Should not be printed.");
   });
});
